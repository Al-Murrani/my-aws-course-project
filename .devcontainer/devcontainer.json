{
  "name": "AWS SAM + CDK + Node.js Dev Container",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:18-bullseye",
  "features": {
    "aws-cli": "latest"
  },
  "postCreateCommand": "npm install -g aws-cdk && curl -Lo install.sh https://raw.githubusercontent.com/aws/aws-sam-cli/main/scripts/install/install.sh && chmod +x install.sh && ./install.sh --yes && rm install.sh && aws --version && sam --version && cdk --version",
  "customizations": {
    "vscode": {
      "extensions": [
        "amazonwebservices.aws-toolkit-vscode"
      ]
    }
  }
}
